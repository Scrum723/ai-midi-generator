# Core (existing + required for AI + live MIDI)
mido>=1.3.0
customtkinter>=5.2.0
python-rtmidi>=1.5.8

# AI / LLM (multi-provider structured output)
openai>=1.30.0
anthropic>=0.30.0
instructor>=1.3.0   # excellent for Pydantic + LLM structured outputs across providers
pydantic>=2.0.0

# Optional but recommended for convenience / preview
python-dotenv>=1.0.0
keyring>=25.0.0       # secure key storage (macOS keychain etc.)

# Audio preview (optional - requires system fluidsynth + sf2 for high quality)
# pyfluidsynth
# pyaudio  # or sounddevice
numpy>=1.24  # recommended for fallback synth preview when no fluidsynth/sf2 (safe to omit; preview will still work via fluidsynth or report gracefully)

# Dev / test (optional)
# pytest

# Packaging (for complete .app on macOS - optional)
# py2app  # pip install py2app; python setup.py py2app
